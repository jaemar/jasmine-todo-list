%h2
  TODO Lists
= link_to "New", new_list_path, class: 'btn btn-primary'
%table.table.table-striped.table-hover.row-fluid
  %thead
    %tr
      %th
        Title
      %th
        List
      %th
        Remark
      %th
        Status
      %th
        Actions
  %tbody
    - @lists.each do |list|
      %tr
        %td
          = list.title
        %td
          = list.list ? simple_format(list.list.gsub('\r\n', '\n')) : "No List Yet!"
        %td
          - if list.remark.present?
            list.remark
          - else
            = "-"
        %td
          - if list.status.present?
            list.status
          - else
            = "-"
        %td#actions
          -#= link_to "Show", list_path(list), class: 'btn btn-success'
          = link_to "Edit", edit_list_path(list), class: 'btn btn-primary'
          = link_to "Delete", list_path(list), class: 'btn btn-danger', method: :delete
